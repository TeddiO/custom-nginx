version: '3'
services:
  nginx:
    image: docker.pkg.github.com/teddio/custom-nginx/custom-nginx:master

    ports:
      - 80:80
      - 443:443

    volumes:
    - /var/www/:/var/www/
    - /etc/nginx/:/etc/nginx/
    - /var/log/nginx/:/var/log/nginx/
    - /etc/ssl/certs:/etc/ssl/certs